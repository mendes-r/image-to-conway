package image.to.conway.image.scaler;

import java.awt.image.BufferedImage;

public class NearestNeightborScale implements ImageScale {

    /**
     * Scale image using nearest neighbor.
     *
     * @param originalImage  Image
     * @param scaleX scale magnitude in the x-axis
     * @param scaleY scale magnitude in the y-axis
     */
    @Override
    public BufferedImage scale(BufferedImage originalImage, float scaleX, float scaleY) {
        return null;
    }
}
